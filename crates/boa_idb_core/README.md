# boa_idb_core

The backend-neutral IndexedDB protocol, key, schema, engine, and transaction
algorithms used by the API and storage backends.

It contains no Boa JavaScript bindings. Run its tests with
`cargo test -p boa_idb_core`.
