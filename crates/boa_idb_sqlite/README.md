# boa_idb_sqlite

The SQLite-backed `BackendFactory` and transaction implementation for durable
IndexedDB storage.

It is selected by the WPT runner with `--backend sqlite`. Run its tests with
`cargo test -p boa_idb_sqlite`.
