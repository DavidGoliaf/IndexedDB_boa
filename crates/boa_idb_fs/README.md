# boa_idb_fs

Filesystem support for IndexedDB persistence, including externalized storage
used by backends in the workspace pipeline.

It is an implementation detail behind the backend crates. Run its tests with
`cargo test -p boa_idb_fs`.
